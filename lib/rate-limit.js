const clients=new Map(),WINDOW=60_000,LIMIT=20;
export function checkRateLimit(identity='unknown'){const now=Date.now(),current=clients.get(identity);if(!current||now-current.start>=WINDOW){clients.set(identity,{start:now,count:1});return{allowed:true,remaining:LIMIT-1};}current.count++;if(clients.size>1000)for(const[key,value]of clients)if(now-value.start>=WINDOW)clients.delete(key);return{allowed:current.count<=LIMIT,remaining:Math.max(0,LIMIT-current.count),retryAfter:Math.ceil((WINDOW-(now-current.start))/1000)};}
